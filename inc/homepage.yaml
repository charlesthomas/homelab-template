homepageName:
  when: "{{ homepageEnabled }}"
  type: str
  default: "{{ name }}"

homepageDescription:
  when: "{{ homepageEnabled }}"
  type: str

homepageGroup:
  when: "{{ homepageEnabled }}"
  type: str

homepageIcon:
  when: "{{ homepageEnabled }}"
  type: str
  default: "{{ homepageName | lower }}"

